
int main()
{
    int Att = 0;
    // 변수를 선언하면서 동시에 특정 값을 넣는것을 '초기화' 라고 한다. ex) int att = 10;
    // 한편 변수를 선언해놓고 나중에 값을 넣는것을 '대입'이라고 한다.  ex) int att; att = 10;
    // 오류를 내지 않으려면 반드시 변수만들때 처음부터 '대입'이 아니라 '초기화'를 해야한다. 초기화할때 뭘 넣을지 모를 때 가장 좋은방법 - 초기화 할때 값 0 넣어주자
}

